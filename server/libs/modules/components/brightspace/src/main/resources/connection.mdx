## Connection Setup

### Register a Brightspace OAuth 2.0 Application

1. In Brightspace, open **Admin Tools → Manage Extensibility → OAuth 2.0**.
2. Register a new application with the redirect URI shown in the connection dialog and the
   `core:*:*` scope.
3. Copy the **Client ID** and **Client Secret** into the connection together with your
   instance URL.
