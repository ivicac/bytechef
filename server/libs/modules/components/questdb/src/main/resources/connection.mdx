## Connection Setup

### Connect to QuestDB

1. Use the HTTP URL of your QuestDB server (port 9000 by default).
2. Enter the username and password of the QuestDB user (admin/quest by default for
   self-hosted instances).
