## Connection Setup

### Connect to an LDAP Server

1. Use the LDAP URL of your directory server (e.g. ldap://ldap.example.com:389 or
   ldaps://ldap.example.com:636).
2. Enter the **bind DN** and password of an account that is allowed to search the
   directory.
