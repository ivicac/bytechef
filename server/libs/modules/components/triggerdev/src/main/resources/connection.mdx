## Connection Setup

### Get Trigger.dev Secret Key

1. In the Trigger.dev dashboard, open your project **environment → API Keys**.
2. Copy the **secret key** (tr_...) into the connection. For self-hosted instances also
   set the instance URL.
