## Connection Setup

### Create Splunk HEC Token

1. In Splunk, open **Settings → Data Inputs → HTTP Event Collector**.
2. Create a new token and enable the HTTP Event Collector.
3. Copy the token into the connection together with the HEC URL (port 8088 by default).
