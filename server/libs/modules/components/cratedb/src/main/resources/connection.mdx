## Connection Setup

### Connect to CrateDB

1. Use the HTTP URL of your CrateDB cluster (port 4200 by default).
2. Enter the username and password of a CrateDB user with access to the queried tables.
