## Connection Setup

### Deploy 1Password Connect

1. Deploy a [1Password Connect server](https://developer.1password.com/docs/connect) in
   your infrastructure.
2. Issue an **access token** for the Connect server with access to the required vaults.
3. Copy the token into the connection together with the Connect server URL.
