## Connection Setup

### Connect to Temporal

1. For a self-hosted Temporal server, enable the HTTP API (default port 7243) and use its URL.
2. For **Temporal Cloud**, create an API key under **Settings** -> **API Keys** and use your namespace endpoint URL.
3. Copy the server URL (and API key, if any) into the connection.
