## Connection Setup

### Create CloudConvert API Key

1. Create an account on [CloudConvert](https://cloudconvert.com/).
2. Open the [Authorization dashboard](https://cloudconvert.com/dashboard/api/v2/keys).
3. Create a new API key with the `task.read` and `task.write` scopes.
4. Copy the key and paste it into the connection.
