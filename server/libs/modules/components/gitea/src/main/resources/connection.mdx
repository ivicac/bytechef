## Connection Setup

### Create Gitea Access Token

1. In Gitea, open **Settings → Applications**.
2. Generate a new **access token** with the required scopes.
3. Copy the token into the connection together with the instance URL.
