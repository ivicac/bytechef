## Connection Setup

### Create Bubble API Token

1. Open your app in the [Bubble editor](https://bubble.io/).
2. Go to **Settings** and select the **API** tab.
3. Enable the **Data API** and select the data types to expose.
4. Generate an API token, copy it, and paste it into the connection together with your app name.
