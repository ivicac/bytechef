## Connection Setup

### Connect to Dagster

1. For **Dagster+**, create a user token under **Organization Settings** -> **Tokens** and use your deployment host, e.g. `https://myorg.dagster.cloud/prod`.
2. For **open-source Dagster**, use your webserver host, e.g. `http://localhost:3000`; no token is required unless you added auth.
