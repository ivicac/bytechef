interface DataSyncTestStepProps {
    draftWorkflowId: string;
}

/**
 * Placeholder for the Test step. Task 11 replaces this body; the wizard already passes the real prop, so
 * this file will not need to change again when that lands.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function DataSyncTestStep({draftWorkflowId}: DataSyncTestStepProps) {
    return null;
}
