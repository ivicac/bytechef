import {DataSync} from '@/shared/middleware/graphql';

interface DataSyncMappingStepProps {
    dataSync: DataSync;
}

/**
 * Placeholder for the Mapping step. Task 11 replaces this body; the wizard already passes the real prop, so
 * this file will not need to change again when that lands.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function DataSyncMappingStep({dataSync}: DataSyncMappingStepProps) {
    return null;
}
