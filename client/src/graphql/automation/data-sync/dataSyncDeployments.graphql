query dataSyncDeployments($workspaceId: ID!) {
    dataSyncDeployments(workspaceId: $workspaceId) {
        id
        name
        dataSyncId
        dataSyncTitle
        projectId
        environmentId
        enabled
        projectVersion
        triggerType
        workflowId
        tags {
            id
            name
        }
        lastExecutionDate
    }
}
