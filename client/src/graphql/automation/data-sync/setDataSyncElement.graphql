mutation setDataSyncElement($input: SetDataSyncElementInput!) {
    setDataSyncElement(input: $input) {
        id
        kind
        componentName
        componentVersion
        operationName
        parameters
        connectionId
    }
}
