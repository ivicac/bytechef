query aiHubToolApprovalRules($workspaceId: ID!) {
    aiHubToolApprovalRules(workspaceId: $workspaceId) {
        id
        workspaceId
        toolKind
        componentName
        toolName
        mode
    }
}
